% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/writeSWBMInputs.R
\name{write_SWBM_sp_days_file}
\alias{write_SWBM_sp_days_file}
\title{Write SWBM Stress Period Days File}
\usage{
write_SWBM_sp_days_file(
  num_days_df,
  output_dir,
  filename = "stress_period_days.txt",
  verbose = TRUE
)
}
\arguments{
\item{num_days_df}{A data frame with one row per stress period and one or more columns (e.g., stress period number, number of days).
Column names will be included in the output file.}

\item{output_dir}{Directory where the file will be written.}

\item{filename}{Character. Output file name. Default is \code{"stress_period_days.txt"}.}

\item{verbose}{Logical. If \code{TRUE}, prints a message indicating the file is being written. Default is \code{TRUE}.}
}
\value{
None. Writes the stress period days file to disk.
}
\description{
Writes the number of days in each stress period (month) for the Soil Water Budget Model (SWBM).
The output file is used to inform the model how many days are in each stress period.
}
\examples{
\dontrun{
num_days <- data.frame(Period = 1:12, Days = c(31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31))
write_SWBM_sp_days_file(num_days)
}
}
