% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/readSWBM.R
\name{read_swbm_block}
\alias{read_swbm_block}
\title{Read a Block from the SWBM Input File}
\usage{
read_swbm_block(file_path, block_name)
}
\arguments{
\item{file_path}{Character. The path to the SWBM input file.}

\item{block_name}{Character. The name of the block to read.}
}
\value{
A named list where the keys are the parameter names from the block and the values are the corresponding settings.
The values are returned as numeric if they can be coerced to numeric, otherwise as characters.
}
\description{
This function reads a specific block from the Soil Water Budget Model (SWBM) input file and returns the key-value pairs as a named list.
The function searches for the specified block, which can appear anywhere in the file, and parses the key-value pairs in that block.
}
\details{
This function searches for a block in the SWBM input file using the block's "BEGIN" and "END" delimiters (e.g., \verb{BEGIN DISCRETIZATION} and \verb{END DISCRETIZATION}).
It collects all the key-value pairs inside the block, ignoring comments and empty lines. Key-value pairs can appear in any order.
}
