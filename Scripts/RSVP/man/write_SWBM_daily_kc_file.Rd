% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SWBM_crop_coefficents.R
\name{write_SWBM_daily_kc_file}
\alias{write_SWBM_daily_kc_file}
\title{Write SWBM Daily Crop Coefficients File}
\usage{
write_SWBM_daily_kc_file(
  daily_kc_df,
  output_dir,
  filename = "kc_values.txt",
  verbose = TRUE
)
}
\arguments{
\item{daily_kc_df}{A data.frame as produced by \code{create_daily_crop_coeff_df()},
containing a \code{Date} column (character "YYYY-MM-DD") and one column per land-use
type (e.g. \code{Alfalfa_Kc}, \code{Grain_Kc}, etc.).}

\item{output_dir}{Character. Path to the directory where the file should be written.}

\item{filename}{Character. Name of the output file (default \code{"kc_values.txt"}).}

\item{verbose}{Logical. If \code{TRUE}, prints a message indicating which file is being written.}
}
\value{
Invisibly returns \code{NULL}.  The primary effect is the side-effect of writing
\code{daily_kc_df} to disk.
}
\description{
Writes a daily table of crop coefficients (Kc) to a space-delimited text file
suitable for ingestion by the Soil Water Budget Model (SWBM).
}
\details{
This is a thin wrapper around \code{write_SWBM_file()}, which uses:
\itemize{
\item space (\code{" "}) as the field separator
\item no quoting of values
\item inclusion of column names but no row names
}
}
\examples{
\dontrun{
# Assume you have already generated daily_kc_df:
daily_kc_df <- create_daily_crop_coeff_df(
  start_date = as.Date("2021-01-01"),
  end_date   = as.Date("2021-12-31")
)
# Write it out:
write_SWBM_daily_kc_file(
  daily_kc_df = daily_kc_df,
  output_dir  = "SWBM_inputs",
  filename    = "year1_kc.txt",
  verbose     = TRUE
)
}
}
