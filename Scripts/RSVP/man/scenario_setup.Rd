% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario_dev.R
\name{scenario_setup}
\alias{scenario_setup}
\title{Set Up Scenario Object}
\usage{
scenario_setup(scen, start_year = 1991)
}
\arguments{
\item{scen}{A named list representing the scenario to be configured. Must include:
\itemize{
\item \code{name} - character; scenario name (used for output directory naming).
\item \code{type} - character; scenario type: one of \code{"BASECASE"}, \code{"UPDATE"}, or \code{"PRMS"} (case-insensitive).
}}

\item{start_year}{Numeric year indicating the start of the model period. Defaults to \code{1991}.}
}
\value{
A modified version of the input \code{scen} list with additional scenario metadata.
}
\description{
Initializes input directories, model start/end dates, and the output scenario directory
for a groundwater-surface water model scenario. This function supports several predefined
scenario types and performs final time discretization calculations based on the selected
model period.
}
\details{
The behavior varies depending on \code{scen$type}:
\describe{
\item{\code{"BASECASE"}}{
Uses reference input data from the "basecase" folder in the Scenario directory.
Sets the model end date to 2024-09-30.
}
\item{\code{"UPDATE"}}{
Uses the latest update directory from \code{data_dir['update_dir', 'loc']} (via \code{latest_dir()}).
Derives the model end date from the folder name (converted to a date), minus one day.
}
\item{\code{"PRMS"}}{
Uses input data from the \code{"PRMS"} subfolder within \code{data_dir['input_files_dir', 'loc']}.
Sets a fixed model end date of 2023-09-30.
}
}

The following components are added to the \code{scen} list:
\itemize{
\item \code{input_dir}: path to scenario input files.
\item \code{start_date}: start date, derived from \code{start_year} via \code{get_model_start()}.
\item \code{end_date}: end date, as described above.
\item \code{num_stress_periods}: total number of model stress periods (typically monthly).
\item \code{num_days}: vector of days in each stress period.
\item \code{scen_dir}: output directory for writing scenario-specific files.
}
}
\examples{
scen <- list('name'='basecase', 'type'='basecase')
scen <- scenario_setup(scen)
}
