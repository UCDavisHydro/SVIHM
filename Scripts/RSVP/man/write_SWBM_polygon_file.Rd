% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/writeSWBMInputs.R
\name{write_SWBM_polygon_file}
\alias{write_SWBM_polygon_file}
\title{Write SWBM Polygon File}
\usage{
write_SWBM_polygon_file(
  df,
  output_dir,
  filename = "polygons_table.txt",
  sep = "\\t",
  quote = FALSE,
  row.names = FALSE,
  col.names = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{df}{A \code{data.frame} as returned by \code{read_SWBM_polygon_file()},
possibly containing additional \code{*_txt} columns.}

\item{output_dir}{Character. Directory in which to write the file.}

\item{filename}{Character. File name, default \code{"polygons_table.txt"}.}

\item{sep}{Character. Field separator. Default: tab (\code{"\t"}).}

\item{quote}{Logical. Quote character fields? Default: \code{FALSE}.}

\item{row.names}{Logical or character. Whether to write row names. Default: \code{FALSE}.}

\item{col.names}{Logical. Whether to write column names. Default: \code{TRUE}.}

\item{verbose}{Logical. If \code{TRUE}, prints a message before writing. Default: \code{TRUE}.}
}
\value{
Invisibly returns \code{NULL}.  Side effect: writes the file.
}
\description{
Writes a SWBM polygon table to disk, \strong{excluding} any of the helper
"_txt" columns added by \code{translate_SWBM_codes()}.  Only the original
code and numeric fields (in the standard SWBM order) are written.
}
\examples{
\dontrun{
poly   <- read_SWBM_polygon_file("polygons_table.txt", landcov, trib)
# ... manipulate poly as needed ...
write_SWBM_polygon_file(
  df         = poly,
  output_dir = "outputs",
  filename   = "polygons_table.txt"
)
}
}
