% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/writeSWBMInputs.R
\name{read_SWBM_polygon_file}
\alias{read_SWBM_polygon_file}
\title{Read SWBM Polygon File and Add Text Translations}
\usage{
read_SWBM_polygon_file(
  filename,
  landcover_desc,
  tributary_desc,
  header = TRUE,
  sep = "",
  stringsAsFactors = FALSE,
  ...
)
}
\arguments{
\item{filename}{Character. Path to the polygon file (passed to \code{read.table}).}

\item{landcover_desc}{Data.frame with columns \code{id} and \code{Landcover_Name};
used to translate \code{SWBM_LU} to \code{LU_txt}.}

\item{tributary_desc}{Data.frame with columns \code{subws_ID} and \code{subws_name};
used to translate \code{subws_ID} to \code{subws_txt}.}

\item{header}{Logical. Does the file have a header row? Default: \code{TRUE}.}

\item{sep}{Character. Field separator. Default: any whitespace (the \code{""} default for \code{read.table}).}

\item{stringsAsFactors}{Logical. Should character columns be converted to factors? Default: \code{FALSE}.}

\item{...}{Additional arguments passed to \code{read.table()}.}
}
\value{
A \code{data.frame} containing all the original columns plus, where applicable:
\itemize{
\item \code{LU_txt}     - text for \code{SWBM_LU}
\item \code{IRR_txt}    - text for \code{SWBM_IRR}
\item \code{WS_txt}     - text for \code{WATERSOURC}
\item \code{subws_txt}  - text for \code{subws_ID}
}
}
\description{
Reads the SWBM polygon table, then, if present, adds human-readable text columns
for any of the code fields \code{SWBM_LU}, \code{SWBM_IRR}, \code{WATERSOURC},
and \code{subws_ID} by calling \code{translate_SWBM_codes()}.
}
\examples{
\dontrun{
landcov <- read.table("landcover_table.txt", header=TRUE)
trib    <- read.table("tributary_desc.txt", header=TRUE)
poly    <- read_SWBM_polygon_file(
  filename        = "polygons_table.txt",
  landcover_desc  = landcov,
  tributary_desc  = trib
)
head(poly)
}
}
