% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SWBM_SFR_files.R
\name{streamflow_curtailment}
\alias{streamflow_curtailment}
\title{Apply a Daily Streamflow Curtailment to Irrigation Inflows}
\usage{
streamflow_curtailment(flows, percent, date_start, date_end, streams = "ALL")
}
\arguments{
\item{flows}{A list with two data frames:
\describe{
\item{\code{irr}}{Data frame of irrigation-available inflows; first column must be Date.}
\item{\code{non_irr}}{Data frame of irrigation-reserved inflows; same structure as \code{irr}.}
}}

\item{percent}{Numeric in [0,1]; the same fraction of \strong{each day's} flow to move.}

\item{date_start}{Date or character; first day (inclusive) of curtailment window.}

\item{date_end}{Date or character; last day (inclusive) of curtailment window.}

\item{streams}{Character vector of flow-column names to affect, or \code{"ALL"} (default)
to use every column except the first (date) column.}
}
\value{
The same \code{flows} list, but with:
\itemize{
\item \code{flows$irr}  reduced by \code{percent * original} on each day in the window;
\item \code{flows$non_irr} increased by that same amount.
}
}
\description{
Moves a fixed percentage of each day's flow from the \code{irr} (available) table to the
\code{non_irr} (reserved) table for a specified inclusive date range.
}
\examples{
subws_inflows <- process_sfr_inflows(scen, file)
subws_inflows <- streamflow_curtailment(subws_inflows,
                                        percent= 1,
                                        date_start = "2021-09-10",
                                        date_end = "2021-10-25")
}
